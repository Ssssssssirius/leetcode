class Solution {
public:
    string reverseString(string s) 
    {
        int i=0;
        int j=s.size()-1;
        while(i<s.size()/2)
            {
                swap(s[i],s[j]);
                i++;
                j--;
            }
        return s;
    }
    
};
